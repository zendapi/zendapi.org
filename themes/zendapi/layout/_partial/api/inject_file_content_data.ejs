<%
let file = page.file;
let files = page.files;
if (!file.includes) {
    file.includes = [];
}
file.includes.map(function(include){
   if (include.refid) {
       include.url = url_for_api_entity(include.refid);
   }
});

%>
window.API_FILE_CONTENT_DATA = <%- JSON.stringify(file)%>