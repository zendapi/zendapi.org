<%
let file = page.file;
let files = page.files;

file.namespaces.map(function(namespace){
    namespace.url = url_for_api_entity(namespace.refid);
});
file.classes.map(function(cls){
    setup_class_info_obj(cls)
});
file.typedefs.map(function(typedef){
    setup_typedef_info_obj(typedef);
});
file.enums.map(function(enumitem){
    enumitem.url = url_for_entity_detail(enumitem.containerId, enumitem.id);
});

file.funcs.map(function(func){
    setup_func_info_obj(func);
});
file.defines.map(function(macro){
    setup_macro_info_obj(macro);
});
file.variables.map(function(variable){
    setup_var_info_obj(variable);
    //    console.log(func);
});
%>
window.API_FILE_CONTENT_DATA = <%- JSON.stringify(file)%>