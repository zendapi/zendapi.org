<%
let items = [];
page.namespaces.map(function(namespace){
    items.push({
        name: namespace.name,
        url: url_for_api_entity(namespace.refid),
        description: namespace.briefDescription
    })
});
%>

window.API_NAMESPACES_LIST_DATA = <%- JSON.stringify(items)%>